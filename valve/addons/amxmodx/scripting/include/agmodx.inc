#if defined _agmodx_included
	#endinput
#endif
#define _agmodx_included

native bool:ag_vote_add(const name[], const function[]);
native bool:ag_vote_remove(const name[]);
native bool:ag_vote_exists(const name[]);
